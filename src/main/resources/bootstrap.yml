spring:
  application:
    name: netflix-gateway

server:
  port: 8091

zuul:
  routes:
    user:
      path: /user/**
      serviceId: NETFLIX-USER
#      url: http://localhost:8080
#      stripPrefix: true
