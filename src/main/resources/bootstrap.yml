spring:
  application:
    name: netflix-gateway
  profiles:
    active:
      dev
server:
  port: 8091
zuul:
  sensitiveHeaders: Cookie,Set-Cookie
  routes:
    user:
      path: /user/**
      serviceId: NETFLIX-USER
